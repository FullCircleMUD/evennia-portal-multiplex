# Progress

Running log of milestones with links to evidence. Reverse chronological — newest first.

## 2026-09-04 (latest)

- **Repository scaffolded.** Library-standards structure only: `pyproject.toml`, `runtests.py`, the
  `src/` layout, `tests/` infrastructure on Evennia's settings defaults, the `log.py` shim writing to
  `portalmultiplex.log`, `CLAUDE.md`, `README.md` and the `docs/` set. Two smoke cases in
  [test-plan.md](test-plan.md), both covered and green.

  No library code, no public surface, and nothing about the design agreed beyond the one-line purpose
  in [README.md](../README.md). The `docs/` documents carry their structure and no content.
